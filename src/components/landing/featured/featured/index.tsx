import './landing-featured.styl'

/**
 * Featured Title
 *
 * Use to express title card category
 *
 * @param children
 */
const Featured = ({ children }) => (
  <h2 className="landing-featured">{children}</h2>
)

export default Featured
